@if (IsCurrentStep)
{

    
    if (!string.IsNullOrWhiteSpace(StepTitle))
    {
        <MudText Typo="Typo.h6" Class="mb-4">@StepTitle</MudText>
    }

    if (ChildContent != null)
    {
        @ChildContent
    }  
}

